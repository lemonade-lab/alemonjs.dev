---
title: V2.1.97
description: 新增事件上下文能力，补充连接、交互和目标媒体 API
authors: ningmengchongshui
tags: [alemonjs]
image: https://i.imgur.com/mErPwqL.png
hide_table_of_contents: false
---

# 更新记录

以下记录根据 A LemonJS 主仓库 `2.1.57` 至 `2.1.97` 的版本提交整理。期间部分版本只包含修复、兼容性调整或构建发布变更，因此按版本和功能归纳。

## 2.1.57–2.1.65：定时任务、Discord 与路由能力

| 版本   | 主要更新                                                     |
| ------ | ------------------------------------------------------------ |
| 2.1.57 | 新增定时任务 API、定时任务存储和 `useMe` 扩展                |
| 2.1.58 | 支持 Discord Components，包括 Embed、Modal、Select 和 Button |
| 2.1.60 | 补充兼容性导出，修复路由兼容问题                             |
| 2.1.61 | 扩展 Discord API 和消息发送能力                              |
| 2.1.62 | 支持按平台分离路由                                           |
| 2.1.63 | 修复路由平台匹配                                             |
| 2.1.65 | 扩展通用工具函数                                             |

## 2.1.66–2.1.80：路由、交互与消息能力增强

| 版本          | 主要更新                                              |
| ------------- | ----------------------------------------------------- |
| 2.1.66        | 调整环境变量和启动配置处理                            |
| 2.1.67        | 重构事件处理链和消息监控，增强事件 trace 与异步上下文 |
| 2.1.68        | 扩展 Node 相关路由能力                                |
| 2.1.69        | 新增链式路由构建器、参数解析、校验和 fallback 提示    |
| 2.1.70        | 更新脚手架模板和 `alemonc` 命令                       |
| 2.1.71–2.1.72 | 修复交互事件路由匹配                                  |
| 2.1.74        | 扩展按钮参数                                          |
| 2.1.75        | 支持 QQ Bot 主动消息和更多 QQ 事件                    |
| 2.1.76        | 优化智能回复和相似路由提示                            |
| 2.1.77        | 优化路由边界匹配，增加路由测试                        |
| 2.1.78        | 扩展事件字段、消息 API 和 QQ 适配器兼容性             |
| 2.1.79        | 增加更多事件快捷字段，统一多平台字段处理              |
| 2.1.80        | 新增 `useRoute`，扩展路由上下文与参数访问             |

## 2.1.81–2.1.92：运行时、生命周期与发布体系

| 版本   | 主要更新                                                           |
| ------ | ------------------------------------------------------------------ |
| 2.1.81 | 改善 Koa 服务管理、应用加载、运行时 Store 和 HTTP 路由             |
| 2.1.82 | 调整 Logger 行为                                                   |
| 2.1.83 | 完善应用生命周期、事件错误处理和订阅执行链；引入协议 V1 与进程拆分 |
| 2.1.84 | 优化脚手架、依赖和应用周期兼容性                                   |
| 2.1.85 | 优化 Koa Router、HTTP 页面和运行时 React 兼容层                    |
| 2.1.86 | 修复 QQ 全量消息和 HTTP 路由问题                                   |
| 2.1.87 | 增强应用加载兼容性、全局运行时能力和客户端处理                     |
| 2.1.88 | 新增发布脚手架和 `alemonc publish` 能力                            |
| 2.1.89 | 优化发布脚本和发布参数                                             |
| 2.1.90 | 重构客户端、平台进程、IPC 和 CBP 运行时                            |
| 2.1.91 | 更新发布能力并补充英文 README                                      |
| 2.1.92 | 修复 Windows 发布流程                                              |

## 新增事件上下文

新增 `createContext` 和 `configureContext`，用于实现多轮对话、分步表单、验证码确认等有状态交互流程。

上下文支持：

- 使用 `scope` 按用户、频道等维度隔离会话；
- 使用 `initialState` 保存每个会话的状态；
- 使用 `expiresIn` 设置过期时间；
- 使用 `conflict` 处理同一作用域的重复会话；
- 使用 `action.close()` 关闭上下文；
- 使用 `action.pass()` 将当前事件继续交给普通路由。

详细说明请查看[事件上下文](/docs/alemonjsDocs/core/context)。

事件上下文系统在 `2.1.92` 的开发历史中落地，后续 `2.1.93` 对 Context 类型、注册校验和依赖结构进行了整理。

## 2.1.93–2.1.97：平台目标、QQ 与运行时更新

| 版本   | 主要更新                                                                                                   |
| ------ | ---------------------------------------------------------------------------------------------------------- |
| 2.1.93 | 整理依赖并调整 Context 注册、事件类型和应用加载                                                            |
| 2.1.94 | 强化平台配置声明和包配置加载                                                                               |
| 2.1.95 | 新增 `useConnection`、`useInteraction`；扩展目标对象、交互事件和媒体发送；OneBot 增加配置与 WebSocket 能力 |
| 2.1.96 | 更新 DB 扩展、HTTP 路由、QQ 入群请求和多平台适配器                                                         |
| 2.1.97 | 支持端口自动递增，改善启动时的端口冲突处理                                                                 |

## 新增 `useConnection`

查询当前机器人连接状态：

```ts
const [{ getStatus }] = useConnection()
const result = await getStatus({ BotId: 'bot-id' })
```

## 新增 `useInteraction`

确认按钮、命令等平台交互事件：

```ts
const [{ ack }] = useInteraction()
const result = await ack({
  InteractionId: 'interaction-id',
  code: 0
})
```

## `useMedia` 能力扩展

媒体 Hook 现在支持 `url`、`data`、`filePath`、`fileId` 四种来源，并增加面向目标对象的 `media.send()`：

```ts
await media.send({
  target: { targetId: '123', scope: 'group' },
  type: 'image',
  url: 'https://example.com/image.png'
})
```

一次媒体操作只能提供一种媒体来源。

## `useMessage` 发送失败提示

当适配器返回空结果，或返回结果中没有 `ResultCode.Ok` 时，框架会记录 WARN 日志。业务代码仍应通过返回结果的 `code` 判断发送是否成功。

## 相关提交

- [Context 系统](https://github.com/lemonade-lab/alemonjs/commit/74e83fcc6b1051c908c6b018cdd036df02262c4c)
- [连接与交互 Hook](https://github.com/lemonade-lab/alemonjs/commit/992278c15b2fbd48d9d1ca29abe21a4fabfa6a5e)
- [消息发送诊断](https://github.com/lemonade-lab/alemonjs/commit/13cb3788e4504ed4c483b991bb67238d58aa8c14)
